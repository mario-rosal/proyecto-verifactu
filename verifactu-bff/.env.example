# ─────────────────────────────────────────────────────────
# Veri*Factu BFF — .env.example (copiar a .env y rellenar)
# ─────────────────────────────────────────────────────────

# Entorno y puerto
NODE_ENV=development
PORT=3001

# CORS: lista blanca (coma-separado). Ej: http://localhost:8080,https://dashboard.tu-dominio.com
# Si se deja vacío o no definido, en dev se usará origin:true (refleja el Origin).
CORS_ORIGINS=

# ── JWT (dashboard / API JWT) ───────────────────────────
# SECRET actual (obligatorio). Usa una cadena larga y aleatoria.
JWT_SECRET=<SET_ME_LONG_RANDOM>
# Rotación opcional: durante la ventana de gracia, el guard aceptará también este secreto.
# Cuando promociones, mueve este valor a JWT_SECRET y vacía esta línea.
# JWT_SECRET_NEXT=<SET_ME_LONG_RANDOM>

# ── Tickets de descarga del conector (HMAC) ─────────────
# SECRET actual (obligatorio). Cadena larga y aleatoria.
DOWNLOAD_TICKET_SECRET=<SET_ME_LONG_RANDOM>
# Rotación opcional (mismo proceso que JWT).
# DOWNLOAD_TICKET_SECRET_NEXT=<SET_ME_LONG_RANDOM>

# Nota: NO subas tu .env real al repo. Este archivo es solo plantilla.
